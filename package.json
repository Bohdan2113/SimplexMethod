{
  "name": "lp-data-viewer",
  "version": "1.0.0",
  "description": "Linear Programming Data Viewer - відображення даних з data.json",
  "main": "index.html",
  "scripts": {
    "clean": "rimraf dist",
    "copy-assets": "cpx \"src/**/*.{html,css,json}\" dist",
    "watch:assets": "nodemon --watch src --ext html,css,json --exec \"npm run copy-assets\"",
    "build:ts": "tsc",
    "watch:ts": "tsc --watch",
    "build": "npm run clean && concurrently \"npm run build:ts\" \"npm run copy-assets\"",
    "serve": "live-server dist --port=3000 --open=./index.html",
    "dev": "npm run clean && npm run copy-assets && concurrently \"npm run watch:ts\" \"npm run watch:assets\" \"npm run serve\"",
    "start": "npm run build && npm run serve"
  },
  "keywords": [
    "typescript",
    "linear-programming",
    "data-visualization",
    "html",
    "css"
  ],
  "author": "DO Lab 1",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cpx": "^1.5.0",
    "live-server": "^1.2.2",
    "nodemon": "^3.1.10",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
